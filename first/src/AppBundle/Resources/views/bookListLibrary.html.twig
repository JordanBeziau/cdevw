{% extends "base.html.twig" %}
{% block body %}
  <section class="container">
    <h1 class="title is-1">{{ title }}</h1>
    {{ form_start(form) }}
      <input type="submit" class="button is-link" value="Remove">
    {{ form_end(form) }}
  </section>
{% endblock %}